<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hyj.demo.toolbardemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.FullScreenActivity"
            android:theme="@style/fullScreen" />
        <activity
            android:name=".ui.BannerImmersionAcitivity"
            android:theme="@style/fullScreen" />
        <activity android:name=".ui.StatusBarSameColorActivity" />
        <activity
            android:name=".ui.FragmentStatusBarActivity"
            android:theme="@style/fullScreen"></activity>
    </application>

</manifest>